{
  "name": "ts-starter",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "oxlint packages apps",
    "lint:fix": "oxlint packages apps --fix",
    "format": "oxfmt packages apps",
    "format:check": "oxfmt packages apps --check",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "oxfmt": "^0.5.0",
    "oxlint": "^1.24.0",
    "prettier": "^3.6.2",
    "turbo": "^2.5.8",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
